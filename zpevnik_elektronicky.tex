%Optimalizovano pro elektronickou verzi:
% - s preferovaným zobrazením na dvě stránky - monitor
% - alternativně na jednu s nastaveným přesným rozměrem stránky - čtečky, tablety, ... 
% - Je potřeba doladit zobrazení písniček, které se na 1 stránku nevešly
% - nově (21.4.2020) je v příkazu \esong implementováno taky vložení obrázku stejného jména jako soubor písničky!

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% ZÁKLADNÍ NASTAVENÍ STRÁNKY %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[twoside,
			   11pt,					% Základní velikost písma: 10pt, 11pt, 12pt
			   notitlepage]
			   {article}

%NASTAVENÍ VELIKOSTI STRÁNKY - GEOMETRY
\usepackage[a5paper, 					% Pro čtečku volit [papersize={135mm, 182mm}, pro běžné použití a5paper
			top = 0.5cm,				% horní okraj
			left = 0.5cm,				% levý okraj
			right = 0.5cm,				% pravý okraj
			bottom = 1.5cm]				% spodní okraj
			{geometry}
			
\usepackage[utf8]{inputenc} 			% píšeme v utf-8, aby to pěkně jelo i linuxákům
\usepackage[]{babel}				% nějaké další potřebné balíčky- čeština
\usepackage[T1]{fontenc}					%pro polštinu -např Hej sokoly
\usepackage{guitar} 					% knihovna pro akordy
\usepackage{multicol} 					% knihovna pro více sloupců
\usepackage{graphicx} 					% knihovna pro obrázky
\usepackage{setspace} 					% knihovna aby šlo měnit řádkování v průběhu stránky
\usepackage{refcount} 					% knihovna pro počítání rozdílů stránek mezi referencemi. Potřebná pro automatické přidávání prázdných bílých stránek.
\usepackage{calc}						% Knihovna pro nějaké výpočty a matematiku
\usepackage{currfile}					% Knihovna pro získání jména současného souboru -- aby mohl vzít obrázek
   
%INTERAKTIVNÍ OBSAH - HYPERREF
\usepackage[pdfusetitle, 				% Nastavení jména PDF souboru (třeba v záhlaví dokumentu) 
			hidelinks, 					% Odkazy nebudou zvýrazněny
			unicode=true, 				% Kódování znaků v bočním obsahu - aby se zobrazily všechny správně (unicode)
			pdfdisplaydoctitle=true,	% Aby se zobrazoval v záhlaví název dokumentu
			pdftoolbar=false,			% Skryje některé nástroje PDF prohlížeče
			pdfmenubar=false,			% Skryje některé menu PDF prohlížeče
			pdfpagelayout=TwoPageLeft,	% defaultní rozložení stránky
			pdfwindowui=false]			% Zase něco zakáže
			{hyperref}
			
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% DEFINICE PŘÍKAZŮ, MAKER A FUNKCÍ %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{\pagedifference}[2]{		% spočítá rozdíl stránek, na kterých jsou 2 dané záložky
	\number\numexpr\getpagerefnumber{#2}-\getpagerefnumber{#1}\relax}
  
\newcommand*\getlength[1]{\number\numexpr#1 / 65536 * 2540 / 7227} %vrátí systémový rozměr (př. rozměr strany) v mm, aby se dal vložit do textu

% Podsekce (subsection) budou číslovány, ale číslo se nikde nezobrazí. Dá se však použít k odkazování se na danou sekci.
\makeatletter
	\def\@seccntformat#1{	
		\csname @seccntformat#1\expandafter\endcsname\csname the#1\endcsname\quad
	}
	\def\@seccntformatsubsection#1#2{}
	\def\numberline#1{}
\makeatother

% POUŽÍVANÉ PŘÍKAZY PRO SAZBU ZPĚVNÍKU
\newcommand{\ch}[1]{\guitarChord{\textbf{#1}}}		% \ch pro akord
% PŘIDÁNO: Přepínač, který si pamatuje, jestli už byl obrázek vložen
\newif\ifpicinserted

% PŘIDÁNO: Přepínače pro vkládání obrázků
\newif\ifpicinserted
\newif\ifdisablepic


\newcommand{\song}[1]{                              
    \gdef\picInsertedFlag{0}
    \gdef\picDisabledFlag{0}
    \clearpage\newpage 
    \ifnum \pagedifference{start:\the\numexpr\value{subsection}+1}{end:\the\numexpr\value{subsection}+1}>0 
        \ifodd \number\numexpr\value{page}
            \relax
        \else
            \ 
            \clearpage\newpage
        \fi 
    \fi
    \label{start:\the\numexpr\value{subsection}+1}  
    
    % 1. Safely drop an invisible target into the TOC file using the numeric counter
    \addtocontents{toc}{\protect\hypertarget{toc:\the\numexpr\value{subsection}+1}{}}
    
    % 2. Link the title back to that exact numeric target
    \subsection[#1]{\hyperlink{toc:\the\numexpr\value{subsection}+1}{#1}}  
    
    \begin{enumerate}
} 

\newcommand{\pic}{							
    \ifnum\picDisabledFlag=0\relax % Tady chyběl \relax!
        \ifnum\picInsertedFlag=0\relax % Tady chyběl \relax!
            \IfFileExists{{images/\currfilebase}.jpg}{%
                \ifdim\dimexpr\pagegoal-\pagetotal\relax>6cm\relax
                    \begin{center} 
                        \vspace*{\fill}
                        \scalegraphics{{images/\currfilebase}.jpg}
                        \vspace*{\fill}
                    \end{center}
                    \gdef\picInsertedFlag{1}%
                \fi
            }{}% 
        \fi
    \fi
}

\newcommand{\nopic}{\gdef\picDisabledFlag{1}}


% Nový příkaz pro manuální skrytí obráz

\newcommand{\ve}{\item }							% \ve pro sloku (1. ...)
\newcommand{\cho}[1]{\item[R:]#1}		 		 	% \cho pro refrén R: ...)
\newcommand{\re}[1]{\item[#1]}						% \re pro něco jiného
\newcommand{\add}[1]{\input{songs/#1}}				% \add pro přidání písničky ze složky songs


% Příkaz, který nafituje obrázek na zbývající místo na stránce:
\newlength{\imgwidth}
\newlength{\imgheight}
\newcommand{\scalegraphics}[1]{						
    \settowidth{\imgwidth}{\includegraphics{#1}}
    \settoheight{\imgheight}{\includegraphics{#1}}
    \setlength{\imgwidth}{\minof{\imgwidth}{\textwidth}}
    \setlength{\imgheight}{\minof{\imgheight}{\the\dimexpr\pagegoal-\pagetotal+\baselineskip\relax}}
    \includegraphics[width=\imgwidth, height=\imgheight, keepaspectratio=true]{#1}
}


\newcommand{\esong}{								% \esong pro konec písničky
	\end{enumerate} 
	\label{end:\the\numexpr\value{subsection}} 		% dává značku na konec písničky kvůli dvoustranným
	\pic{}											% vkládá obrázek, pokud existuje
}

\newcommand{\uv}[1]{\quotedblbase #1\textquotedblleft}

%%%%%%%%%%%%%%%%%%%%%%%
% ZBÝVAJÍCÍ NASTAVENÍ %
%%%%%%%%%%%%%%%%%%%%%%%

\widowpenalties 1 10000 				% zakáže zlom stránky uprostřed sloky
\raggedbottom 							% souvisí s widowpenalties; aby TeX potom nezvětšoval mezery mezi řádky

\AtBeginDocument{\renewcommand{\abstractname}{Popis}}	% Název "abstraktu", pokud je vložený
\setcounter{tocdepth}{3} 				% Aby se v obsahu objevily pouze písně (ne sloky atd.)
\newcommand{\folder}{images}			% Název složky s obrázky %%Zrušit!

\author{Mara,  \\ Doplnění po sdílení na overleaf Rádio} 							% Autor zpěvníku
\title{Zpěvník -- verze pro počítač}	% Název zpěvníku
 

%%%%%%%%%%%%%%%%%%%%%
% ZAČÁTEK DOKUMENTU %
%%%%%%%%%%%%%%%%%%%%%
\begin{document}


%\setcounter{page}{1}  					% nastaví čítač stránek. V základu začne od 1, ale když by třeba bylo v plánu připojit ještě obálku...

% Vytvoření automatického nadpisu - název, jméno, datum
\maketitle
\label{obsah}


%\begin{abstract}
%\noindent
%Tato verze zpěvníku je optimalizovaná pro čtečky. 
%Tato verze zpěvníku je optimalizovaná pro počítače a mobily. 
%PDF má rozměry \getlength{\paperwidth} mm x \getlength{\paperheight} mm
%, pro úsporu místa nemá okraje ani čís;stránek
% a má interaktivní klikací obsah. Kliknutím na libovolný nadpis se vrátíte na 1. stranu. 
%Před písničku, která je na 2 strany, je automaticky vkládána volná stránka v případě, že by začínala na sudé stránce.
%\end{abstract}

%Nastavení číslování stránek pro úvod a obsah
\pagestyle{empty}	%bez čísla
\newpage

%\pagestyle{plain}	%s číslem

%Vytvoření automatického obsahu. Správně se vygeneruje vždy až při druhém překladu.
\tableofcontents

%Nastavení číslování stránek pro samotný zpěvník
%\pagestyle{empty}
\pagestyle{plain}

%vložení .txt souboru se seznamem písniček
\input{all_songs.txt} % přidání celého textového souboru s písněmi - více písní najednou

\end{document}